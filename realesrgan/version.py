# GENERATED VERSION FILE (default placeholder)
# This file is normally overwritten by `python setup.py` (see write_version_py()
# in setup.py), which stamps the real git hash and timestamp at build time.
# A default copy is committed here so that `import realesrgan` works straight
# from a git checkout / extracted zip, without requiring a build/install step
# first (useful for Colab: !pip install -r requirements.txt without
# `pip install -e .` still gets a working import).
__version__ = '0.3.0'
__gitsha__ = 'unknown'
version_info = (0, 3, 0)
